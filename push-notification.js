var push = require("web-push");

let vapidkey = {};  // to generate vapid keys run $ node ./generate-vapid.js

push.setVapidDetails("mailto:hello@example.com",vapidkey.publicKey,vapidkey.privateKey);

let sub = {}; // this is user key, generated by the browser
push.sendNotification(sub);